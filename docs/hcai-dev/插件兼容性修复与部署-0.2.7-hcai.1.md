# 插件兼容性修复与部署：0.2.7-hcai.1

日期：2026-09-20。基线 main `b62f68088f1996378bdd4f2514e877e3c6ad5e08`，其业务文件与已部署的 `4a11804440570f7621360d9c3683e03d725a40a1` 一致。本次用户授权修复并在 jp-01、jp-02、tw、美国滚动发布，沿用已批准的逐台短暂维护。独立工作树实施，不混入原工作区未提交的文档或删除。

## 问题与修复

标准 semver 将 `0.2.7-hcai` 视为低于 `0.2.7` 的预发布版本，原插件要求 `>=0.2.7 <0.3.0` 因此被拒绝。

修复限定于宿主范围比较：普通上游版本约束使用 HCAI 上游基线；只识别 `-hcai` 或 `-hcai.N`，保留真实预发布版本和显式 HCAI 修订约束的语义。tested 声明继续精确匹配，协议、Transport API、UI Bridge 校验不变。不自动启用插件、不修改插件清单，不调用真实上游。

版本文件升级为 `0.2.7-hcai.1`，不覆盖已有发布标签和附件；没有新增数据库迁移。系统自动更新比较目前只比较三段数字，不会提示 HCAI 修订差异，必须以部署版本、构建提交及哈希识别；本次不扩展自动更新逻辑。

## 验证与部署进度

已通过：定向 HCAI 下限/上限、精确版本、数字修订、元数据、非法版本、真实预发布、显式修订约束、tested 状态及三种协议不匹配测试；前端 lint、306 文件/2293 测试、生产构建（含类型与 locale 检查）。`resolve-version.sh` 返回 `0.2.7-hcai.1`，`git diff --check` 通过。

初始 Go 命令因外部网络等待被终止，未记作通过；使用本机 Go 1.27.0 和缓存依赖（GOPROXY=off）后定向测试成功。用户随后明确要求按小修复直接替换，故完整后端测试未完成、不作为本次阻断。embed 二进制构建和节点验收在执行后追加。

按用户本次二进制修复上线指令在本地构建并保留源码提交与校验信息；GitHub PR、main 合并、远程标签/Release 发布未执行，不将本地修订包冒充 GitHub 已发布产物。

计划顺序：jp-01 → jp-02 → tw → 美国；每台备份原二进制和配置，原子替换并重启对应 systemd 服务，验证运行中哈希、版本、本机健康、HCAI 资源、公开设置和未认证 API 401。失败恢复旧程序并暂停后续节点。用量日志超时按用户此前明确指示记录为已知风险；其他启动/迁移/健康失败仍阻断。

## 构建产物

- 源码提交：`628652c475927a8da891bbeee6f441098fa51054`，分支 `codex/fix-hcai-plugin-compat`，本地提交未推送。
- Linux amd64 静态 ELF，Go 1.27.0、CGO_ENABLED=0、embed、trimpath，ldflags 注入完整版本、提交和 UTC 构建时间；构建成功。
- 本地产物目录：独立工作树 `backend/dist/hcai0271/`。
- 二进制 SHA-256：`375966dbb83371949156f74958c06fd602eb884f0145aca3ad756f8349050608`。
- gzip SHA-256：`d46b0fbf61d4adca18f8446b74983ffa03c792ba314c3f91790552f709bc97eb`。
- HCAI 静态首页哈希与上一版一致：`2e694ef65eb536c0200870ebee906e45e773b66fe26f63a43a95701daca8b97f`。
- 相对 main 无迁移变更，回退只需恢复上一版程序；不执行数据库写入修复。

## 节点结果（北京时间）

| 节点 | 完成时间 | PID | 验收 |
| --- | --- | --- | --- |
| jp-01 | 05:39:13 | 1598726 | 三轮基础检查通过；窗口内 6 条应用错误记录，继续观察已知用量日志风险 |
| jp-02 | 05:40:55 | 1074326 | 三轮基础检查通过；窗口内 3 条用量日志错误记录，按授权不阻断 |
| tw | 05:43:35 | 2729647 | 三轮基础检查通过；窗口内无应用错误 |
| 美国 | 05:46:30 | 3239232 | 三轮基础检查通过；窗口内无应用错误 |

留档目录：日本/美国 `/opt/sub2api/releases/20260920-hcai0271/`，台湾 `/www/wwwroot/ai-prod.hctopup.com/releases/20260920-hcai0271/`；各自 `previous/` 保留旧程序、配置和可用环境文件，限制为 root 访问。原 ExecStart 路径不变。日志查询显式使用 UTC，避免此前时区导致的窗口扩大。

日本两个节点的错误类别已核验为此前已知的用量日志写入超时，本次没有修复或掩盖；台湾和美国本次观察窗口没有应用错误。四台滚动发布已完成，未操作 jp-03、共享数据库或 Redis 服务，未使用 Docker。

插件状态预期为版本范围兼容；只声明测试过 0.2.7 的插件仍显示未经声明测试，需要管理员确认启用。没有自动启用插件或执行 OAuth 真实请求，不能将部署基础验收等同插件端到端验收。
