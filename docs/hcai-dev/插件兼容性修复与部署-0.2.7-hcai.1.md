# 插件兼容性修复与部署：0.2.7-hcai.1

日期：2026-09-20。基线 main `b62f68088f1996378bdd4f2514e877e3c6ad5e08`，其业务文件与已部署的 `4a11804440570f7621360d9c3683e03d725a40a1` 一致。本次用户授权修复并在 jp-01、jp-02、tw、美国滚动发布，沿用已批准的逐台短暂维护。独立工作树实施，不混入原工作区未提交的文档或删除。

## 问题与修复

标准 semver 将 `0.2.7-hcai` 视为低于 `0.2.7` 的预发布版本，原插件要求 `>=0.2.7 <0.3.0` 因此被拒绝。

修复限定于宿主范围比较：普通上游版本约束使用 HCAI 上游基线；只识别 `-hcai` 或 `-hcai.N`，保留真实预发布版本和显式 HCAI 修订约束的语义。tested 声明继续精确匹配，协议、Transport API、UI Bridge 校验不变。不自动启用插件、不修改插件清单，不调用真实上游。

版本文件升级为 `0.2.7-hcai.1`，不覆盖已有发布标签和附件；没有新增数据库迁移。系统自动更新比较目前只比较三段数字，不会提示 HCAI 修订差异，必须以部署版本、构建提交及哈希识别；本次不扩展自动更新逻辑。

## 验证与部署进度

已通过：定向 HCAI 下限/上限、精确版本、数字修订、元数据、非法版本、真实预发布、显式修订约束、tested 状态及三种协议不匹配测试；前端 lint、306 文件/2293 测试、生产构建（含类型与 locale 检查）。`resolve-version.sh` 返回 `0.2.7-hcai.1`，`git diff --check` 通过。

初始 Go 命令因外部网络等待被终止，未记作通过；使用本机 Go 1.27.0 和缓存依赖（GOPROXY=off）后定向测试成功。用户随后明确要求按小修复直接替换，故完整后端测试未完成、不作为本次阻断。embed 二进制构建和节点验收在执行后追加。

按用户本次二进制修复上线指令在本地构建并保留源码提交与校验信息；GitHub PR、main 合并、远程标签/Release 发布未执行，不将本地修订包冒充 GitHub 已发布产物。

计划顺序：jp-01 → jp-02 → tw → 美国；每台备份原二进制和配置，原子替换并重启对应 systemd 服务，验证运行中哈希、版本、本机健康、HCAI 资源、公开设置和未认证 API 401。失败恢复旧程序并暂停后续节点。用量日志超时按用户此前明确指示记录为已知风险；其他启动/迁移/健康失败仍阻断。
